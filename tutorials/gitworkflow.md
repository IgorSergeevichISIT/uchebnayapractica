Лабораторной работы №3: "Магия Ветвей" 
Это задание научит  главной боли и главному инструменту разработчика — как работать параллельно и не поубивать друг друга при слиянии кода.

***


# Лабораторная работа №3: Ветвление, слияние и решение конфликтов

**Цель:** Научиться создавать ветки, переключаться между ними, сливать изменения и, самое главное, не паниковать при возникновении `Merge Conflict`.

**Инструменты:** Git (терминал), VS Code (или любой редактор кода).

***


### 🛠 Блок 0: Подготовка рабочего места (Установка Git)

Перед началом работы убедитесь, что Git установлен и настроен.

#### **Для Windows**
1.  Скачайте установщик с официального сайта: [git-scm.com/download/win](https://git-scm.com/download/win).
2.  Запустите `.exe` файл.
3.  **Важно:** Во время установки нажимайте "Next", но на этапе выбора редактора (*Choosing the default editor used by Git*) выберите **VS Code** (если он у вас стоит) или оставьте Nano/Notepad. Не выбирайте Vim, если не умеете из него выходить!
4.  В настройке *Adjusting your PATH environment* выберите **"Git from the command line and also from 3rd-party software"** (обычно это рекомендуемая опция).
5.  После установки откройте PowerShell или командную строку (`Win + R` -> `cmd`) и введите:
    ```bash
    git --version
    ```
    Если увидели версию (например, `git version 2.43.0`), значит всё отлично.

#### **Для macOS**
1.  Откройте Терминал.
2.  Введите команду `git --version`.
3.  Если Git не установлен, macOS сама предложит установить **Xcode Command Line Tools**. Согласитесь и дождитесь установки.
4.  Альтернативный вариант (через Homebrew): `brew install git`.

#### **Для Linux (Ubuntu/Debian)**
1.  Откройте терминал.
2.  Введите:
    ```bash
    sudo apt update
    sudo apt install git
    ```

#### **Первоначальная настройка (ОБЯЗАТЕЛЬНО)**
Git подписывает каждый ваш коммит вашим именем и почтой. Если это не настроить, GitHub не зачтет вам активность.
В терминале введите (подставьте свои данные):
```bash
git config --global user.name "Ivan Ivanov"
git config --global user.email "ivanov@example.com"
```
*Важно: используйте ту же почту, на которую зарегистрирован ваш GitHub-аккаунт.*

***

### 📚 Блок 2: Словарик Айтишника (Основные определения)

Чтобы понимать, что происходит, нужно говорить на одном языке.

| Термин | Простым языком | Аналогия из жизни |
| :--- | :--- | :--- |
| **Repository (Репозиторий/Репо)** | Папка с вашим проектом, где Git следит за каждым файлом. | Дневник, в который вы записываете все изменения. |
| **Commit (Коммит)** | Точка сохранения. Фиксация изменений в коде с комментарием. | Сейв в игре перед боссом. |
| **Branch (Ветка)** | Параллельная версия проекта. Позволяет работать над новой фичей, не ломая основной код. | Черновик документа, который вы копируете, чтобы попробовать переписать главу, не портя оригинал. |
| **Master / Main** | Основная, главная ветка проекта. Здесь лежит "чистовой" рабочий код. | Опубликованная версия вашей курсовой работы. |
| **Merge (Слияние)** | Процесс объединения изменений из одной ветки в другую (например, из черновика в чистовик). | Вставка проверенного абзаца из черновика в основной документ. |
| **Merge Conflict (Конфликт)** | Ситуация, когда Git не может сам слить ветки, потому что в одном и том же месте кода написаны разные вещи. | Два человека одновременно редактировали одну и ту же фразу в Google Doc, и теперь непонятно, чей вариант оставить. |
| **Pull Request (PR)** | Запрос на слияние вашей ветки с основной. Это просьба "Посмотрите мой код и добавьте его в проект". | Сдача домашки на проверку учителю перед тем, как её зачтут. |
| **Origin** | Ссылка на удаленный репозиторий (на сервере GitHub), откуда вы скачали проект. | "Облако", где лежит копия вашего проекта. |

***



## 🏗 Часть 1: Создание параллельной реальности (Ветки)

Представьте, что вы пишите код, а ваш коллега хочет исправить ошибку. Вы не можете править один файл одновременно в одной ветке — вы будете мешать друг другу. Для этого нужны ветки (branches).

1.  Откройте терминал в папке вашего репозитория.
2.  Убедитесь, что вы в ветке `main` (или `master`):
    ```bash
    git checkout main
    git pull origin main  # Скачиваем свежие изменения, если они есть
    ```
3.  Создайте новую ветку для "новой фичи" (например, добавление информации о хобби):
    ```bash
    git checkout -b feature/hobby
    ```
    *(Команда `-b` создает ветку и сразу переключает вас на нее)*
4.  Создайте файл `hobby.txt` и напишите туда 3 строчки о ваших увлечениях.
5.  Закоммитьте изменения:
    ```bash
    git add hobby.txt
    git commit -m "Add my hobbies list"
    ```
6.  **Внимание:** Сейчас этот файл существует ТОЛЬКО в ветке `feature/hobby`.
    Вернитесь в `main` и проверьте, что файла там нет:
    ```bash
    git checkout main
    ls  # Файла hobby.txt не должно быть!
    ```

***

## 🤝 Часть 2: Слияние (Merge) без боли

Теперь нам нужно влить вашу работу из `feature/hobby` обратно в главную ветку `main`.

1.  Находясь в ветке `main`, выполните команду слияния:
    ```bash
    git merge feature/hobby
    ```
2.  Если вы все сделали верно, Git напишет `Fast-forward` и файл `hobby.txt` появится в `main`.
3.  Удалите ненужную ветку (мы же все слили):
    ```bash
    git branch -d feature/hobby
    ```

***

## ⚔️ Часть 3: Искусственный конфликт (Merge Conflict)

Теперь смоделируем ситуацию, когда два человека правят одну и ту же строку. Это вызовет конфликт.

1.  **Создаем ветку А:**
    ```bash
    git checkout -b conflict-branch-a
    ```
2.  Создайте файл `conflict.txt` и напишите внутри: `Коты лучше собак.`
3.  Закоммитьте:
    ```bash
    git add conflict.txt
    git commit -m "Cats are better"
    ```
4.  Вернитесь в `main`:
    ```bash
    git checkout main
    ```
5.  **Создаем ветку Б:**
    ```bash
    git checkout -b conflict-branch-b
    ```
6.  Создайте ТОТ ЖЕ файл `conflict.txt`, но напишите внутри: `Собаки лучше котов.`
7.  Закоммитьте:
    ```bash
    git add conflict.txt
    git commit -m "Dogs are better"
    ```
8.  **Слияние с конфликтом:**
    Попробуйте влить `conflict-branch-a` в вашу текущую ветку `conflict-branch-b`:
    ```bash
    git merge conflict-branch-a
    ```
    😱 **Вы увидите сообщение:** `CONFLICT (add/add): Merge conflict in conflict.txt`. Git не знает, кто прав: коты или собаки.

***

## 🚑 Часть 4: Решение конфликта

1.  Откройте файл `conflict.txt` в VS Code. Вы увидите странные символы:
    ```text
    <<<<<<< HEAD
    Собаки лучше котов.
    =======
    Коты лучше собак.
    >>>>>>> conflict-branch-a
    ```
2.  Вам нужно **принять решение**. Удалите служебные метки (`<<<`, `===`, `>>>`) и оставьте тот текст, который считаете правильным (или объедините их: `Коты и собаки крутые`).
3.  Сохраните файл.
4.  Сообщите Git, что конфликт решен:
    ```bash
    git add conflict.txt
    git commit -m "Resolve conflict: animals are friends"
    ```
5.  Поздравляю! Вы выжили в первом мердж-конфликте.

***

## ✅ Задание для отчета

1.  Загрузите (push) изменения в ваш удаленный репозиторий.
2.  В отчете приложите скриншот терминала в момент возникновения конфликта (где написано `CONFLICT`).
3.  Приложите скриншот "Графа" вашего репозитория (В VS Code можно использовать расширение "Git Graph" или команду `git log --graph --oneline --all`).

***

### 💡 Совет для понимания
 **Визуализация Git:** [LearnGitBranching](https://learngitbranching.js.org/?locale=ru_RU) (поиграйте уровни 1-4 в "Основах").
